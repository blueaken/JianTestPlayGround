the difference between the recursive and bottom up solution is the bottom up solution check the diff of L and R subtree
on each depth calculation, so as to achieve check if balanced function on an updated maxDepth function.